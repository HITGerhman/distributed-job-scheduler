package domain

// Instance is kept as an alias for the canonical JobInstance type so the
// M1 directory skeleton can use the shorter name without breaking existing code.
type Instance = JobInstance
